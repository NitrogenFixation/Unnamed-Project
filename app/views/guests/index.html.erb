<%= form_for(@guest) do |f| %>
   <div class="field">
	<%= f.label :name %>: <%= f.text_field :name %>
   </div>
   <div class="actions">
	<%= f.submit "Create" %><br>
	<%= link_to 'Guestbook', "guestbook" %>
   </div>
<% end %>